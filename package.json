{
  "name": "agents-monitor",
  "version": "0.1.0",
  "description": "Local browser monitor for Codex and Claude Code agents",
  "author": "Agents Monitor contributors",
  "license": "MIT",
  "scripts": {
    "start": "node src/server.cjs --open",
    "serve": "node src/server.cjs",
    "check": "node --check src/monitor.cjs && node --check src/insights.js && node --check src/renderer.js && node --check src/server.cjs && powershell -NoProfile -Command \"[scriptblock]::Create((Get-Content -Raw 'src/pin-window.ps1')) | Out-Null; [scriptblock]::Create((Get-Content -Raw 'src/dock-window.ps1')) | Out-Null\"",
    "test": "node tests/monitor.test.cjs && node tests/insights.test.cjs"
  }
}
